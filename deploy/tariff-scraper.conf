# /etc/tariff-scraper.conf — tariff-scraper 环境变量配置
# 省份列表(空格分隔), 名称须与页面一致(可用 --list-provinces 查询)
#
# 防封建议: 每次运行只配一个省 —— 页面默认视图自带「全网资费」页签,
# 单省一次运行即覆盖 全国+该省 全部所需数据, 请求量最小。
# 确需多省对比时, 不要加到同一次运行里拉全部, 而是再建一个 timer 实例
# (换 EnvironmentFile 并错开 OnCalendar 时刻), 见 DEBIAN12.md。
TARIFF_PROVINCES="江苏省"
